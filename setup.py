from setuptools import setup


setup(name='stream2msg',
      version='1.0',
      description='Converts tcp socket bit stream to messages.',
      url='https://github.com/theahura/socket-wrapper',
      author='Amol Kapoor',
      author_email='theahura@gmail.com',
      license='MIT',
      packages=['stream2msg'],
      zip_safe=False)
